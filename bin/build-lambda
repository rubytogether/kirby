#!/bin/bash
set -euo pipefail

docker build -t aws-lambda:latest -f docker/dockerfile --platform linux/amd64 .
docker run -v "$PWD/target/lambda":/target --rm aws-lambda:latest bash -c "cp * /target/"
